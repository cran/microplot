microplot <- function (object, ...)
  UseMethod("microplot")

if (FALSE) { ## this generic not currently used
as.includegraphics <- function (object, ...)
  UseMethod("as.includegraphics")
}

MSWord <- function (object, filetype=c("docx","html"), ...)
  UseMethod("MSWord")

